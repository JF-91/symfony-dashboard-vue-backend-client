{% extends 'base.html.twig' %}

{% block title %}New Post{% endblock %}

{% block body %}
<div class="container">
    <h1 class="is-primary is-center ">Create new Post</h1>

    <div class="form">
        <div class="form-group" >
            {{ include('post/_form.html.twig') }}
        </div>

        <a href="{{ path('app_post_index') }}">back to list</a>
    </div>
</div>
{% endblock %}
